/**
 * 
 */
package com.ss.basics.four;

/**
 * @author ronh
 *
 */
public class ProductOnHand {
	static Integer onHand = 5;				// Initial on hand counter default
	static Integer queueSize = 10;
	
	// To regulate the loop
	static int consumerCount = 1000;	// sleep time default
	static int consumerMax = 20;		// maximum for loop default
	static int producerCount = 100;	// sleep time default
	static int producerMax = 20;		// maximum for loop default
}
